# 2026-06-13 App Icon

## 变更
- 使用内置 imagegen 生成“花火记”App 图标主视觉：花火核心、花瓣光圈与摄影/插画灵感意象。
- 填充 `FlowerFireNote3/Assets.xcassets/AppIcon.appiconset`：
  - iOS any / dark / tinted 三个 1024x1024 图标。
  - mac 16 到 512 点位的 1x / 2x 图标。
- 更新 `AppIcon.appiconset/Contents.json`，让 Xcode 引用新增 PNG。

## 校验
- 所有图标文件均为 RGB PNG，无透明通道。
- 已确认 `Contents.json` 可被 JSON 解析。
- 已在 iPhone 17 模拟器下完成 Debug 构建并启动验证，XcodeBuildMCP 返回 `SUCCEEDED`，无 warnings/errors。
